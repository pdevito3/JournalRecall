namespace JournalRecall.AI.EntityFrameworkCore;

/// <summary>
/// Assembly marker for the AI EF Core satellite. Real types (conversation persistence) land in
/// issue 0007; this keeps the Phase 0 project non-empty and discoverable for assembly scanning.
/// </summary>
public static class AssemblyMarker;
